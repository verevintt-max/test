services:
  - type: web
    name: warehouse-app
    env: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: DATABASE_URL
        value: sqlite:///./warehouse.db
      - key: PYTHONUNBUFFERED
        value: "1"
    healthCheckPath: /api/health

